# 🧠 多大脑 · 秒级切换（小焦调度中心）

> 你的架构图（原样保留），机制由 `brain_manager.py` 落地。

```text
┌──────────────────────────────────────────────────────────────┐
│                           用户界面                            │
│                    (Web / CLI / 小焦对话窗口)                │
└────────────────────────────────┬─────────────────────────────┘
                                 │ "生成一个视频，内容是..."
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│                        小脑（调度中心）                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ① 意图理解 + 大脑选择                                 │  │
│  │ "用户想生成视频" → 查询大脑仓库 → 匹配到 "视频大脑"    │  │
│  └───────────────────────────────────────────────────────┘  │
│                          │                                   │
│                          ▼                                   │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ② 显存调度器（核心切换逻辑）                         │  │
│  │ 当前状态：聊天大脑（Qwen）运行，占 3.5GB              │  │
│  │ 目标状态：视频大脑（Wan2.1）需要 5GB                   │  │
│  │ 执行切换：                                            │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │  │
│  │  │ 休眠聊天大脑│→ │ 唤醒视频大脑│→ │ 切换到视频模式│     │  │
│  │  └────────────┘  └────────────┘  └────────────┘     │  │
│  │  休眠：权重从显存→内存(~2s)  唤醒：权重从内存→显存(~1-2s)│  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────────────────┬─────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│                         大脑仓库                             │
│  A 聊天大脑(Qwen 4B)  B 视频大脑(Wan2.1+ComfyUI)             │
│  C 图像大脑(SD3)【未来】  D 推理大脑(DeepSeek)【未来】        │
│  每大脑：状态(SLEEP/RUN/OFF) · 显存占用 · 唤醒时间            │
└────────────────────────────────┬─────────────────────────────┘
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│              显存管理底层（vLLM睡眠 / torch.cuda / 手动管理） │
│  sleep: 权重→内存  wake: 权重→显存  (进程常驻,不杀)           │
└────────────────────────────────┬─────────────────────────────┘
                                 ▼
┌──────────────────────────────────────────────────────────────┐
│                           执行结果                            │
│  生成完成→保存 → 卸载视频大脑→唤醒聊天大脑 → 用户看到结果      │
└──────────────────────────────────────────────────────────────┘
```

## 机制说明（`brain_manager.py` 已实现框架）
- **小脑 = 调度中心**：`BRAINS` 注册表管理所有大脑（每大脑=模型服务+唯一端口+显存占用+状态）。
- **`switch_to(目标大脑)`**：休眠当前在跑的大脑 → 唤醒目标大脑。**进程常驻不杀**，只做"权重显存↔内存" → 秒级。
- **新增大脑**：在 `BRAINS` 加一项即可被调度（图像/推理/任意模型都兼容）。

## 真正达到「秒级」需要各服务支持权重 offload/onload
框架本身已就绪，实际的速度取决于每个服务的 sleep/wake 能力：

| 大脑类型 | sleep/wake 实现 | 现状 |
|---|---|---|
| 图像/视频（torch：SD3/Wan） | `model.to('cuda'/'cpu')` + `torch.cuda.empty_cache()` | ✅ 可实现 ~1-2s |
| LLM（llama.cpp） | 需 **llama-swap**（多个 llama 进程常驻、秒切）或 vLLM sleep 模式 | ⏳ 下一步接入 |
| 未来 DeepSeek/vLLM | vLLM sleep 模式 | ⏳ 可复用同一框架 |

**当前视频流程已改为用 `brain_manager` 调度**（`video_service` 切换走 `switch_to`）。

## 小脑学习（连所有大脑）
- 小脑向量库（`self_learn/vstore`）可**按大脑打 tag** 存"从哪个大脑学到的"：

```
用户: xxx -> 视频大脑精炼: ...     tag=video_prompt
用户: xxx -> 聊天大脑回答: ...     tag=chat_answer
```

- **只要用哪个大脑，小脑就向它学**；下次同类请求直接命中缓存/学过的内容 → 更快更准。

## 快速体验
```bash
python brain_manager.py        # 交互: switch video / switch chat / state
```
