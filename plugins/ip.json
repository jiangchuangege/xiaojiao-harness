{
  "name": "ip",
  "description": "查询本机公网IP(API插件示例)",
  "tools": [
    {
      "name": "get_ip",
      "description": "查本机公网 IP(只返回地址)。什么时候用：只要一个 IP 字符串；要归属地/ISP/时区请用 net_ip 或 get_ip_info；无参数；输出 IP 文本",
      "parameters": {"type": "object", "properties": {}},
      "method": "GET",
      "url": "https://api.ipify.org",
      "response": "text"
    },
    {
      "name": "get_ip_info",
      "description": "查某个 IP 的归属地/ISP/时区。什么时候用：**已经知道**要查哪个 IP；输入 ip；输出 归属地 JSON。查自己机器的公网 IP 用 net_ip",
      "parameters": {"type": "object", "properties": {"ip": {"type": "string", "description": "IP地址"}}},
      "method": "GET",
      "url": "https://ipapi.co/{ip}/json",
      "response": "json"
    }
  ]
}
