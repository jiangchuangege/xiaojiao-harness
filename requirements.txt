# 小焦 · XiaoJiao Harness — Python 依赖
# 已按实际代码 imports 核对齐全。推荐 Python 3.10+。

# ---------- 核心深度学习（自研 MiniGPT 训练/推理） ----------
torch>=2.0
numpy>=1.24

# ---------- Web / 接口（Flask，端口 5000） ----------
flask>=2.0
requests>=2.28

# ---------- 分词/检索（小脑向量库、记忆、jieba 分词） ----------
jieba>=0.42

# ---------- 可选：8-bit 量化优化器（约省一半显存，加速接力训练） ----------
# 安装失败时训练脚本会自动回退为 torch.optim.AdamW，故列为可选。
# bitsandbytes>=0.41
deepseek-harness>=0.3.0   # DSH 桥接(DSH 深度功能)

# ---------- 可选：功能增强（缺哪个=哪个功能用不了, 不影响核心聊天） ----------
# 🎙️ 播客配音 TTS（Chatterbox 或 ChatTTS 二选一）
chatterbox-tts>=0.8      # 🎙️ 播客配音（如装不上可改用 chattts）
# chattts>=0.2.2
# 🎨 播客/视频封面（SD1.5 图像生成）
diffusers>=0.27
transformers>=4.40
accelerate>=0.30
# 🎵 音乐生成（ACE-Step 只带 API server, 由 music_service 调用）
# 说明：ACE-Step 独立运行, 不需要 pip 装进本环境；ComfyUI 用自带 python_embeded。

# ---------- 说明 ----------
# · 本地模块（不入库）无需 pip 安装：vstore / xiaojiao_app / xiaojiao_harness 等。
# · 大脑模型不写死：放任意 GGUF 到 C:/llama 自动识别, 或配置里填任意 OpenAI 兼容 API。
# · DeepSeek Harness / DSH 插件：走小焦 /v1，无需额外 Python 包。
# · 视频大脑（ComfyUI + Wan2.1）用 ComfyUI 自带 python_embeded，与本文独立。
# · llama-swap（多大脑秒级切换）是独立 exe，非 Python 包。
# · Node.js 用于 .js 插件（plugin_runner.js）。
