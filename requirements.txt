# 小焦 · XiaoJiao Harness — Python 依赖
# 推荐 Python 3.10+ / CUDA 12.x

# ---------- 核心深度学习 ----------
torch>=2.0

# ---------- Web 监控面板 ----------
flask>=2.0

# ---------- HTTP / 本地 LLM 调用 ----------
requests>=2.28

# ---------- 可选：8-bit 量化优化器（约省一半显存，加速“接力训练”） ----------
# 安装失败时训练脚本会自动回退为 torch.optim.AdamW，故列为可选。
# bitsandbytes>=0.41

# ---------- 推理 / 数据通用 ----------
numpy>=1.24
